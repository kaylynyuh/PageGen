import React from "react";
import "./styles.scss";

const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="navbar-buttons">
        <button>Exit</button>
        <button>Save</button>
      </div>
    </nav>
  );
};

export default Navbar;
